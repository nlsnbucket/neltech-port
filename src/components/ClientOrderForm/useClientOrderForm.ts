interface FormData {
    name
    content,
    phone
}

export const useClientOrderForm = () => {
    // const {
    //     foo,
    //     error,
    //     clearError,
    //     setValue,
    //     setError,
    //     onSubmit: onSubmitWrapper
    // } = useForm<FormData>()

    const data: formData = {
        jfdkls
        jfdkls
        jfdkls
        jfdkls
    }

    const onChange = () => {
        // lidar mundança no input 1
        //      limpar erro do input
        //      

    }

    const onSubmit = () => {

    }

    return {
        foo,
        onChange,
        onSubmit: onSubmitWrapper(onSubmit)
    }
}